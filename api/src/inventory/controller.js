const pool = require("../db");
const queries = require("./queries");

const get_inventory = () =>{}
const get_inventory_id = () =>{}
const remove_inventory = () =>{}
const update_inventory = () =>{}


module.exports = {
    get_inventory,
    get_inventory_id,
    remove_inventory,
    update_inventory,
}